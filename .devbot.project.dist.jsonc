{
  "project_name": "__PROJECT_NAME__",
  "harness": "opencode",
  "devbot_dir": ".agents",
  "commit_memory": false,
  // Opt in BY NAME to datasources declared in .devbot.global.jsonc. Only these
  // are registered with this project's harness — the rest cost nothing, not
  // even context.
  "datasources": [],
  "modules": {
    "react": false,
    "signoz": false,
    "svelte": false
  },
  "auto_recover": {
    "max_attempts": 5
  },
  "guards": [
    {
      "regex": "rm -rf",
      "message": "rm -rf is blocked"
    },
    {
      "regex": "sudo .*",
      "message": "sudo requires approval"
    },
    {
      "regex": "git push --force",
      "message": "force push is prohibited"
    }
  ]
}
