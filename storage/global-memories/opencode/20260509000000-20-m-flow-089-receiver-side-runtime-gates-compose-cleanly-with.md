improve-planning iter-12 P1 was the first deployment of the receiver-side runtime-refusal mechanism shape (Prompt-opener gate in po/architect/critic role files: agent inspects the user-prompt at receive time and returns [BLOCKED] if the first sentence does not match the canonical `Write|Update <path> ...` form). Predicted impact: stall reduction. Actual iter-12 impact: stalls 4 → 1 (75% reduction), Process dim 50 → 78, Solver dim 50 → 80, overall grade 81 → 89, **above the predicted ceiling by 3pt**. The new mechanism composed with iter-8 P2 `@tester` verifying-tool-call gate without any interaction defects. Iter-13 evidence confirms the composition holds: 6/6 Prompt opener lines, stalls held at 1, no L3. No interaction effects with iter-12 P3 critic per-block-type checklist (a sibling receiver-side runtime gate at the critic boundary).
Receiver-side runtime gates (post-receive, pre-generation refusal mechanisms) are a high-confidence mechanism class on this evaluation model. They compose cleanly with prior verifying-tool-call gates because both operate at the agent boundary as structural refusal points, not as in-prompt instructions the agent must remember to follow. Diagnostic: for each new mechanism class, run one iteration with the M-FLOW-077 conservative prediction band; if actual exceeds ceiling by ≥2pt, future predictions for the **same mechanism class** can be tightened by 2-3pt. Iter-12 → iter-13 sequence (both ceiling-breaching) confirms the band may be over-cautious for receiver-side / draft-time gate shapes. Pre-emptive heuristic at proposal-design time: when choosing between an orchestrator-side textual rule and a receiver-side runtime gate, prefer the receiver-side gate — the structural refusal mechanism is more reliable than the agent's compliance with text it must read and remember.

- See also: [[memories#M-FLOW-077]] (floor formula with new-defect-class risk band — this entry tightens the band for the receiver-side gate sub-class), [[memories#M-FLOW-087]] (receiver-side enforcement is the only working locus for protocol-compliance gates that the sender reliably skips — this entry is the empirical confirmation), [[memories#M-FLOW-092]] (textual rules don't bind during-generation behaviour on this model — sibling lesson defining the boundary where this mechanism class stops working), `.opencode/agents/{po,architect,critic}.md` (`Prompt-opener gate (MUST)` section, iter-11 P1, commit `0ac566d`), `storage/self-improvement/improve-planning/iteration-12/quality-report.md` §3, `storage/self-improvement/improve-planning/iteration-13/quality-report.md` §3.

improve-planning iter-12 P1 (prompt-opener receiver-side gate in po/architect/critic role files) was the first deployment of the receiver-side runtime-refusal mechanism shape. M-FLOW-077 conservative band predicted iter-12 grade 80–86 with −3pt risk for new mechanism class. Actual iter-12: 89, +3 above ceiling. Stalls 4→1 (75% reduction), Process dim 50→78, Solver dim 50→80. The new gate composed with iter-8 P2 `@tester` verifying-tool-call gate without interaction defects. iter-13 confirmed the composition pattern: iter-13 grade 92 (+1 above ceiling), second consecutive ceiling breach.
Receiver-side runtime gates (where the receiver agent inspects the inbound prompt against a pattern and BLOCKS on mismatch) compose with other receiver-side runtime gates (e.g., `@tester` verifying-tool-call) on independent inspection axes — no interaction defects observed across two consecutive iterations. For each new mechanism class, run one iteration with conservative prediction band; if actual exceeds ceiling, future predictions for the same mechanism class can be tightened by 2–3pt. Diagnostic: when introducing a receiver-side runtime gate, verify it inspects an axis orthogonal to existing gates (prompt-opener form ⊥ first-tool-call type ⊥ third-party-cite presence) — orthogonality is the necessary condition for clean composition.

- See also: [[memories#M-FLOW-077]] (floor formula with new-defect-class risk band — this entry tightens the band for receiver-side gate shapes), [[memories#M-FLOW-087]] (receiver-side enforcement is the working locus — this entry generalises to composition behaviour), `.opencode/agents/{po,architect,critic}.md` (`Prompt-opener gate (MUST)` section, iter-11 P1, commit `0ac566d`; First-tool-call invariant paragraph, iter-12 P2, commit `553109d`), `storage/self-improvement/improve-planning/iteration-12/quality-report.md` §1, `storage/self-improvement/improve-planning/iteration-13/quality-report.md` §1.
