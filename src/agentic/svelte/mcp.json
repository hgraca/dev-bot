{
  "mcp": {
    "svelte": {
      "type": "stdio",
      "command": [
        "bash",
        "-c",
        "mkdir -p .agents/logs && exec npx -y @sveltejs/mcp 2>>.agents/logs/svelte-mcp.log"
      ]
    }
  }
}
