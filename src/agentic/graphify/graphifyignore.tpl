# Graphify-specific ignores (project .gitignore also applies)
graphify-out
# The composer phar (a multi-MB binary at repo root) gets parsed as code by
# graphify's updater and warns on every build (audit-22 NOTE).
composer
