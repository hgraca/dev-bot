# playwright dependency pin (T1.2).
#
# install.sh installs exactly this version; update.sh bumps this file to the
# npm latest on `devbot update`. Updated only via that flow — edit by hand and
# `devbot update` will overwrite it with npm latest.
PLAYWRIGHT_MCP_VERSION=0.0.80
